#pragma once

// Copy this file to include/secrets.h (which is gitignored) and fill in your
// values. INFLUXDB_TOKEN must match INFLUXDB_ADMIN_TOKEN in .env (see
// .env.example), since that's what docker-compose uses to initialize
// InfluxDB's admin token.

// --- WiFi ---
#define WIFI_SSID     "your-wifi-ssid"
#define WIFI_PASSWORD "your-wifi-password"

// --- Device identity (InfluxDB tag) ---
// DEVICE_NAME is set per-board by the PlatformIO environment (see
// platformio.ini: `comfort`, `mushroom`, ...), not here, so one secrets.h
// can flash multiple boards. Add a new [env:NAME] there per board.

// --- InfluxDB 2.x ---
// URL must be reachable from the ESP32 on your LAN. Use the host machine's
// LAN IP (e.g. http://192.168.1.50:8086), NOT localhost, since the board is
// a separate device from wherever docker-compose runs.
#define INFLUXDB_URL    "http://192.168.1.50:8086"
#define INFLUXDB_ORG    "aqm"
#define INFLUXDB_BUCKET "aqm"
#define INFLUXDB_TOKEN  "change-me"
